# Secrets for disunified. Copy to a file the container reads with
# --env-file (Podman/Docker) and keep it out of version control.
STU_SWITCH_USER=stu
STU_SWITCH_PASS=change-me
STU_UNIFI_API_KEY=change-me
